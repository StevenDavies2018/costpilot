{
  "name": "costpilot",
  "version": "1.0.0",
  "description": "CostPilot - Niche-specific receipt scanner and expense tracker for Canadian market",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:ios": "cd apps/ios && npm start",
    "dev:web": "cd apps/web && npm run dev",
    "build:ios": "cd apps/ios && npm run build",
    "build:web": "cd apps/web && npm run build",
    "lint": "cd apps/ios && npm run lint && cd ../web && npm run lint"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
